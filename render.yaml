services:
  - type: worker
    name: discord-bot
    runtime: python
    buildCommand: pip uninstall -y discord.py discord-ext-voice-recv && pip install --no-cache-dir -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
